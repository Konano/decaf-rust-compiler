class Main {
    bool field;

    class Main another;

    int[] foo(int y) {
        int[] q = new int[y];
        int i = 0;

        while (i < y) {
            q[i] = i;
            i = i + 1;
        }

        return q;
    }

    static int main() {
        return this.foo(5).length();    
    }
}